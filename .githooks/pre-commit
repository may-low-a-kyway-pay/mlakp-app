#!/bin/sh
set -eu

files=$(git diff --cached --name-only --diff-filter=ACMR | grep -E '\.(js|jsx|ts|tsx|json|css|md|yaml|yml)$' || true)

if [ -z "$files" ]; then
  exit 0
fi

printf '%s\n' "$files" | xargs npx prettier --write
printf '%s\n' "$files" | xargs git add
